{% extends 'base_private.html.twig' %}

{% block title %} - pré-création d'un group d'exposants{% endblock %}

{% block body %}
    <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold mb-6">Liste des exposants</h1>

        <a href="{{ path('app_exhibitor_group_pre_create') }}" class="rounded bg-white hover:text-gray-700 text-black font-bold py-2 px-4 my-4 border border-black">Créer un groupe d'exposants</a>

        <div class="overflow-x-auto shadow-md rounded-lg mt-6">
            <table class="min-w-full bg-white border border-gray-200">
                <thead class="bg-gray-50 border-b border-gray-200">
                <tr>
                    <th class="py-3 px-6 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Nom du groupe</th>
                    <th class="py-3 px-6 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                {% for exhibitorGroup in exhibitorsGroup %}
                    <tr class="hover:bg-gray-100">
                        <td class="py-4 px-6 whitespace-nowrap text-sm text-gray-500">{{ exhibitorGroup.groupName }}</td>
                        <td class="py-4 px-6 whitespace-nowrap text-right text-sm font-medium">
                            <a href="{{ path('app_exhibitor_group_edit', { id: exhibitorGroup.id }) }}" class="text-blue-600 hover:text-blue-900">Éditer</a>
                            <a href="{{ path('app_exhibitor_group_delete', { id: exhibitorGroup.id }) }}" class="text-red-600 hover:text-red-900 ml-4">Supprimer</a>
                        </td>
                    </tr>
                {% else %}
                <tr>
                    <td colspan="5" class="py-4 px-6 text-center text-sm text-gray-500">Aucun groupe d'exposants trouvé.</td>
                </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}